# Registro de uso de Inteligencia Artificial (IA)
> Este documento registra de manera transparente el uso de de herramientas de Inteligencia Artificial durante el desarrollo del proyecto **LaPlacita**, con el propósito de mantener la transparencia en el proceso de elaboración de la documentación, diseño y desarrollo del sistema, bajo la supervisión directa del equipo de desarrollo.

## Resumen: qué se aceptó y qué se rechazó

- Entradas registradas: 25 (06/08/2026 al 18/09/2026).
- Aceptadas y verificadas por el equipo: 19 (columna Validación con ejecución o confirmación).
- Aceptadas con ajustes del equipo: 3 entradas.
- Entradas con propuestas rechazadas y justificadas: 6 (8 propuestas en total).
- Propuestas rechazadas: ampliar C4 a más detalle; restaurar Árbol de Utilidad en aspectos.md;
  auto-corregir V-01/V-03 el mismo día; microservicios o renombrar módulos;
  declarar corte-vertical.js como sexto contexto; run en rojo en master;
  editar el ADR-0006 ya aceptado; párrafos no sustentables por el equipo.
- Detalle completo por fecha y herramienta en la bitácora (§1).

---

# 1. Bitácora de Interacciones y Resultados

| Fecha | Herramienta | Propósito | Resumen del Prompt (Entrada) | Resultado obtenido | Rechazado | Validación |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **06/08/2026** | Gemini | Elaboración de la estructura inicial del README del proyecto. | *"Generar una estructura base en Markdown para el README organizado con secciones para la descripción, problema, objetivos y documentación."* | Se obtuvo un README organizado con secciones para la descripción y documentación. | — | Revisado y adaptado por el equipo. |
| **08/08/2026** | ChatGPT (OpenIA) | Ampliación y refinamiento de la documentación inicial necesaria. | *"Complemetar con información necesaria la documentación cumpliendo requerimientos específicos de la guía."* | Estructura formal del repositorio, desglose de información completaria para la documentación. | — | Revisado, editado y adaptado por el equipo de desarrollo. |
| **15/08/2026** | Claude (Anthropic) | Organización de la información ya definida (ficha del problema y aspectos de calidad) en las secciones 1-3 de arc42, y estructuración del diagrama C4 de contexto. | *"Ayudar a organizar la información existente e ideas del proyecto en la estructura de las secciones de introducción, restricciones y contexto del arc42, y estructurar esa información como diagrama de contexto en formato Mermaid."* | Información reorganizada en las secciones 1-3 del arc42 y diagrama C4 de contexto estructurado en Markdown. | — | Revisado, corregido y adaptado por el equipo de desarrollo. |
| **15/08/2026** | Claude (Anthropic) | Estructuración del árbol de utilidad en formato de diagrama (Mermaid) a partir de los aspectos de calidad ya definidos. | *"Ayudar a representar el árbol de utilidad ya definido como diagrama en formato Mermaid, en lugar de texto plano."* | Árbol de utilidad estructurado como diagrama Mermaid, organizado por prioridad de los aspectos de calidad. | — | Revisado y adaptado por el equipo de desarrollo. |
| **15/08/2026** | ChatGPT (OpenIA) | Refinamiento y verificación de los escenarios de calidad y su relación con el árbol de utilidad. | *"Completar los escenarios de calidad del proyecto de acuerdo a lo solicitado, asegurando que sean medibles y estén relacionados con los aspectos de calidad."* | Se definieron escenarios con estímulo, fuente, entorno, respuesta esperada y medidas de respuesta. Se estableció la trazabilidad entre `A-01, A-02, A-04, A-05 y A-06` y los escenarios `ESC-01 a ESC-05`. | — | Revisado por el equipo y contrastado con las instrucciones de la actividad y la documentación del proyecto. |
| **23/08/2026** | ChatGPT (OpenAI) | Revisión de la documentación de arquitectura y del registro de uso de IA frente a los criterios de la actividad. | *"Revisar la documentación actual del proyecto y el registro de uso de IA, identificar aspectos que no cumplen completamente con los requerimientos y proponer los ajustes necesarios, incluyendo el registro de decisiones o propuestas rechazadas y su justificación."* | Se identificaron aspectos faltantes en el registro de IA, especialmente la necesidad de documentar las propuestas consideradas y rechazadas, indicando el motivo de rechazo. También se propusieron ajustes para mejorar la trazabilidad entre las sugerencias de IA y las decisiones tomadas por el equipo. | — | Revisado por el equipo de desarrollo. Las recomendaciones fueron evaluadas y se incorporaron únicamente aquellas consideradas pertinentes para el proyecto. |
| **23/08/2026** | ChatGPT (OpenIA) | Revisión del nivel de detalle de los diagramas de arquitectura del proyecto. | *"Analizar si los diagramas C4 actuales deberían ampliarse para representar con mayor detalle los componentes internos del sistema."* | Se planteó ampliar el nivel de detalle de los diagramas para representar más elementos internos de LaPlacita. | Ampliar C4 a mayor detalle — nivel de abstracción actual suficiente para la etapa del proyecto; agregar más detalles dificultaría el mantenimiento de la documentación. | Se rechazó la propuesta. |
| **23/08/2026** | Claude (Anthropic) | Corrección de los criterios no cumplidos identificados en la retroalimentación del evaluador: restricciones, diagrama C4, escenarios de calidad y registro de IA. | *"Revisar los criterios de evaluación no cumplidos e indicar exactamente qué texto agregar en cada archivo para corregirlos."* | Se obtuvieron los textos corregidos para las secciones §2 (restricciones reales), el diagrama C4 con leyenda, los escenarios ESC-01 a ESC-05 completos con el campo «Artefacto» y las entradas corregidas de este registro. | Restaurar sección «Árbol de Utilidad y Escenarios de Calidad» en `docs/aspectos.md` — la decisión del equipo es mantener esa información centralizada únicamente en el arc42; revertir implicaría duplicar contenido que no se puede mantener sincronizado. | Aceptada con ajustes. Se incorporaron las correcciones de restricciones, los escenarios con el campo «Artefacto» y las entradas de este registro. |
| **23/08/2026** | Claude (Anthropic) | Generación del esqueleto ejecutable (servidor base, módulos vacíos y prueba automatizada) siguiendo la estructura de módulos definida en el ADR-0001. | *"Generar el esqueleto ejecutable del backend en Node.js según el estilo monolito modular ya decidido en el ADR: servidor base con endpoint de salud, carpetas vacías por módulo (catálogo, pedidos, pagos, entrega, notificaciones) y una prueba automatizada en verde."* | Servidor HTTP básico (src/index.js) con endpoint /health, cinco carpetas de módulo con index.js vacío documentando su responsabilidad futura, tests/health.test.js y comando único de arranque (node src/index.js) documentado en el README. | — | Revisado y ejecutado por el equipo: node src/index.js y npm test corridos localmente, prueba en verde confirmada (pass 1 / fail 0) antes de subir. |
| **24/08/2026** | Claude (Anthropic) | Revisión de la retroalimentación del evaluador (planilla de equipo, revisión S3) frente al estado real del repositorio, y corrección de los pendientes señalados: enlaces de los requisitos a los escenarios, ratificación del ADR-0001 y ausencia de pipeline. | *"Revisar la retroalimentación del profesor frente a lo que existe en el repositorio, identificar qué falta, y entregar una guía completa de las acciones pendientes para obtener resultados óptimos en la evaluación."* | Se obtuvo una guía priorizada de tres acciones con sus archivos ya corregidos: enlace de `RF-01…RF-06` a los escenarios `ESC-01…ESC-05` correspondientes en `docs/aspectos.md`, cambio del estado del ADR-0001 de "propuesto" a "aceptado" con referencia al commit `37f92bb` que lo implementa, y un pipeline `.github/workflows/ci.yml` que ejecuta `npm test` en Node 18.x y posteriores en cada push y pull request. | — | Se aceptó los enlaces a los escenarios `ESC-01…ESC-05` correspondientes en `docs/aspectos.md` y la implementación, del pipeline `.github/workflows/ci.yml` que ejecuta `npm test` en Node 18.x y posteriores en cada push y pull request, **Pendiente por confirmación** el cambio propuesto del estado del ADR-0001 de "propuesto" a "aceptado" con referencia al commit `37f92bb` que lo implementa. |
| **30/08/2026** | Claude (Anthropic) | Migración de la capa HTTP del backend de Node.js nativo (`http` sin framework) a Next.js (API Routes, App Router), como primer paso hacia una futura versión full-stack sobre Next. | *"Cambiar todo lo que se tiene en Node.js a Next.js, enfocándose por ahora únicamente en el backend."* | Se reemplazó `src/index.js` (servidor `http` plano) por `app/health/route.js` (histórico S4; hoy `app/api/v1/health/route.js` desde S7, ver fila 16/09/2026) (Next.js App Router), conservando la misma ruta `GET /health` y el mismo contrato de respuesta (`{ status: 'ok' }`). Se actualizaron `package.json` (dependencias `next`/`react`/`react-dom` y scripts `dev`/`build`/`start`/`test`), `tests/health.test.js` (invoca el route handler directamente en vez de levantar un servidor HTTP), se agregaron `next.config.mjs`, `jsconfig.json` y `.gitignore`, y se conservó el servidor anterior como referencia en `legacy/index.node-http.js`. Los módulos de dominio (`src/modules/*`) no se modificaron por ser lógica pura sin dependencia del framework HTTP (ADR-0001). | — | Pendiente de validación por el equipo: ejecutar `npm install`, `npm run dev` y `npm test` localmente para confirmar que el endpoint `/health` y la suite de pruebas siguen en verde antes de subir el cambio. |
| **30/08/2026** | Claude (Anthropic) | Diagnóstico de una advertencia de VS Code en `jsconfig.json` ("Option 'baseUrl' is deprecated and will stop functioning in TypeScript 7.0") tras un valor incorrecto introducido manualmente (una URL en vez de `"."`) en la opción `baseUrl`. | *"VS Code lanza la advertencia 'option baseUrl is deprecated and will stop functioning in TypeScript 7.0', ¿qué significa y cómo se soluciona?"* | Se confirmó mediante búsqueda web que `baseUrl` está deprecado desde TypeScript 6.0 y será removido en TypeScript 7.0, y que ya no es necesario desde TypeScript 4.1 en adelante porque `paths` funciona por sí solo cuando sus rutas usan prefijos relativos (`"./*"`). Se eliminó por completo la clave `baseUrl` de `jsconfig.json`, dejando únicamente `paths` con el alias `@/*` → `./src/*`. | — | Aceptada y aplicada directamente por el equipo; se verificó que el alias `@/*` sigue funcionando sin `baseUrl`. |
| **30/08/2026** | Claude (Anthropic) | Diagnóstico de un fallo de `npm test` (`Error: Cannot find module '.../tests'`) al ejecutar el proyecto con Node.js v24, confirmando primero que no dependía de tener `npm run dev` corriendo en paralelo. | *"npm test falla con 'Cannot find module tests' al correrlo con Node v24, aunque npm run dev funciona bien."* | Se identificó mediante búsqueda web que Node 22+ dejó de aceptar una carpeta como argumento posicional de `--test` (la resuelve como module specifier en vez de escanearla como directorio), lo que provoca el error `MODULE_NOT_FOUND`. Se cambió el script `test` de `package.json` de `node --test tests/` a `node --test "tests/**/*.test.js"` (patrón glob explícito entre comillas), compatible con las versiones recientes de Node. | — | Pendiente de confirmación del equipo tras volver a ejecutar `npm test` con el script corregido. |
| **30/08/2026** | Big Pickle (Ollama) | Migración del dominio del backend de CommonJS a JavaScript ESM (`import`/`export`) y ajuste de la versión de Node del pipeline a 22, preparando el proyecto para un stack full-stack sobre Next.js. El frontend y las API Routes `/api/*` quedaron planificados para una iteración posterior. | *"Migra todo el backend a JavaScript ESM (sin TypeScript) y resuelve la incompatibilidad del test runner con Node 18."* | Se añadió `"type": "module"` a `package.json`, se migraron `src/modules/*`, `src/health.js` y `src/corte-vertical.js` (incluido el guard de CLI `require.main` → `import.meta.url`) a `import`/`export`, y se adaptaron los tres archivos de `tests/*` a ESM. Se removió `legacy/` (servidor http nativo obsoleto) y se actualizó `.github/workflows/ci.yml` a `node-version: '22'` y `engines.node` a `>=22`, ya que el runner `node --test` solo soporta patrones glob desde Node 21+. | — | Aplicada por el equipo; `npm test` en verde (8/8), `npm run build`, `npm run dev` + `GET /health` y `node src/corte-vertical.js` verificados. |
| **06/09/2026** | Big Pickle (Ollama) | Implementación de la restricción RES-05 del corte 1: aislamiento estricto por establecimiento sobre el corte vertical, con diagnóstico, ADR, medición y trazabilidad. | *"Ejecuta las fases 1-5 del plan de corte 1: restricciones, diagnóstico, ADRs, implementación de la restricción RES-05 (Aislamiento estricto por establecimiento) sobre el corte vertical, y verificación."* | Se decidió RES-05 en [ADR-0004](../docs/adr/0004-aislamiento-por-establecimiento.md) (repositorio particionado por tienda). Se midió la línea base sobre `812d227` (2/2 accesos cruzados logrados) y se implementó el aislamiento con `tiendaId` obligatorio en `src/modules/{catalogo,pedidos,pagos,entrega,notificaciones}/index.js` y `src/corte-vertical.js`, con `tests/aislamiento.test.js` (4 nuevas pruebas) y `scripts/medir-aislamiento.js`. Post-cambio: 0 accesos cruzados en 300 intentos. Se actualizó arc42 (§5, §6, §9, §10, §11), ADR-0001 (estado y trazabilidad), ADR-0003 (Dockerfile, `sonar-project.properties`, paso SonarCloud en CI), C4 de contenedores (planeados), README, glosario y `correcciones.md`. | — | Pendiente de revisión del equipo: `npm test` (13/13 en verde), `node scripts/medir-aislamiento.js` (salida 0), `node src/corte-vertical.js`, y revisión de cada documento antes de commitear, pushear y etiquetar `corte-1`. |
| **13/09/2026** | Claude/Anthropic (vía OpenCode) + Big Pickle (Ollama, verificación local) | Auditoría S6 de propiedad de datos sobre código actual: recorrer `src/modules/*/index.js` + `src/corte-vertical.js`, listar escrituras cruzadas (`pedido.pin = …`, `cambiarEstado` desde Pagos/Entrega, `import obtenerProducto` en Pedidos) y producir V-01…V-06 con plan. | *"Audita propiedad de datos entre los 5 módulos JS, cita ruta:línea de cada escritura cruzada y propón plan sin romper firmas para S6."* | Tabla V-01 (crítica, `entrega/index.js:15`), V-02 (`pedidos/index.js:7,38`), V-03 (`pagos/index.js:24`, `entrega/index.js:14,29`), V-04 (`tiendaId` duplicado), V-05 (notificaciones vía orquestador), V-06 (orquestador god) en `docs/dominio/auditoria-modularidad.md` + tabla dueño/escribe/lee en `propiedad-de-datos.md`. | Auto-corregir V-01/V-03 hoy (p. ej. crear `asignarPin`) — rompería firmas congeladas de `corte-1` y tests existentes; se deja como plan Corte 2 con test pendiente. | Aceptado tras verificar cada ruta:línea contra HEAD `2675e3f`. |
| **13/09/2026** | Misma sesión IA | Mapa de contextos DDD + lenguaje ubicuo + §8 arc42 + C4 nivel 3 + ADR-0005 + trazabilidad aspectos→contextos. | *"Genera mapa con Customer/Supplier, OHS, Shared Kernel y ACL + §8 + componentes.md + ADR de reajuste enlazando aspectos."* | `docs/dominio/mapa-de-contextos.md` ampliado (SK `tiendaId` + ACL pasarela), `docs/arc42/08-conceptos-transversales.md` + §8 en template, `docs/c4/componentes.md`, `docs/adr/0005-reajuste-contextos-propiedad.md`, tabla Aspecto→Contexto en `aspectos.md`. | 1) Proponer microservicios o renombrar módulos (p. ej. `billing` en inglés) — viola convención española y RES-02/03. 2) Declarar a `src/corte-vertical.js` como sexto contexto — no tiene datos propios. | Aceptado con ajustes del equipo (nombres de contextos = nombres de módulos para no romper ADR-0001). |
| **13/09/2026** | Muse Spark (esta asistencia, vía OpenCode) | Investigación S6 sin modificar repo: clonar ambos repos en temp, cruzar ficha S6 + feedback LaPlacita + código, entregar informe archivo-por-archivo con .md listo para pegar. | *"Clona/inspecciona ambos repos, revisa .md + código + feedback S6 completo, entrega informe archivo-por-archivo en español con contenido listo."* | Este informe (no commiteado): gaps 0/8→8/8, auditoría V-01…V-06, contenidos de `correcciones.md`, `ia.md`, `08*`, `componentes.md`, `0005*`, `auditoria-modularidad.md`, orden de ejecución y mensaje de commit. | Párrafos no sustentables por el equipo — todo el informe debe ser leído, ajustado y sustentado antes de commitear; se descartará cualquier bloque que el equipo no pueda defender. | Pendiente de validación del equipo antes de commitear: cada bloque debe leerse y ajustarse. |
| **13/09/2026** | Claude Sonnet 5 (Claude Code) | Cierre del pendiente V-01/V-03 registrado en `correcciones.md` (deuda de código pospuesta a S7): implementar en el código el dueño único de `pin` y los métodos de intención, sin romper `corte-1`. | *"El código de V-01 y V-03 no se corrigió todavía (solo documentado + planificado)... puedes corregir o terminar de arreglar esos problemas."* | Se agregó `pedidos.asignarPin` (único escritor de `pin`, `obtenerPedido` retorna copia `Object.freeze`) y los métodos de intención `confirmarPago/marcarListo/confirmarEntrega`; se actualizaron `pagos/index.js` y `entrega/index.js` para invocarlos en vez de mutar `pin` directo o llamar `cambiarEstado` con el nombre del estado. Se agregó el test pendiente *"pin inmutable desde fuera"* y se ajustó `tests/aislamiento.test.js` (leía `pedido.pin` de una referencia obsoleta, patrón que V-01 elimina). `npm test`: 14/14 verde; `node scripts/medir-aislamiento.js`: 0/300. Se actualizaron `auditoria-modularidad.md`, `ADR-0005` y `correcciones.md` con la evidencia. | — | Pendiente de validación del equipo (commit y revisión de diffs antes de subir). Sobre SonarCloud: no se intentó configurar `SONAR_TOKEN` porque `sonar.organization`/`sonar.projectKey` en `sonar-project.properties` siguen siendo placeholders — requiere que un miembro del equipo cree/vincule el proyecto en sonarcloud.io con su propia cuenta de GitHub y genere el token. |
| **13/09/2026** | OpenCode (mimo-v2-free) | Revisión de errores de la Semana 5 y planificación de la Semana 6: cruzar la ficha de evaluación S5 con el estado del repositorio, identificar criterios fallidos y planificar correcciones. | *"Analiza los repositorios LaPlacita y feedback, verifica errores S5 y qué se necesita para S6"* | Informe de 3 criterios fallidos en S5: (1) nombre de archivo `correciones.md` en vez de `correcciones.md`, (2) `docs/ia.md` sin columna formal de rechazos, (3) SonarCloud sin `SONAR_TOKEN`. Plan de acción para cada uno. | — | Revisado y validado por el equipo; se incorporaron las correcciones de nombre (commit `0726ed8`) y se planificó la columna de rechazos (este cambio). |
| **16/09/2026** | OpenCode (esta asistencia) | Implementación de rutas HTTP en `app/api/v1/*` siguiendo el contrato `openapi.yaml` v1: endpoints REST (health, catalogo, pedidos, pagos, entrega, notificaciones). Cada ruta delega en el módulo correspondiente de `src/modules/`. | *"Crea las API routes de Next.js App Router para los endpoints definidos en openapi.yaml"* | Archivos creados: `app/api/v1/health/route.js`, `app/api/v1/pedidos/route.js`, `app/api/v1/pedidos/[pedidoId]/route.js`, `app/api/v1/pagos/[pedidoId]/confirmar/route.js`, `app/api/v1/entrega/[pedidoId]/listo/route.js`, `app/api/v1/entrega/[pedidoId]/validar/route.js`, `app/api/v1/notificaciones/route.js`. Se eliminó `app/health/route.js`. | — | Verificado: `npm run dev` y `npm test` en verde; endpoints accesibles en `http://localhost:3000/api/v1/*`. |
| **16/09/2026** | OpenCode (esta asistencia) | Revisión del repositorio para asegurar que la prueba de contrato se integra en `npm test` y en el pipeline CI, e instalación de dependencias. | *"Revisa todo el repositorio para asegurar que el contract-test funciona dentro de npm test y del pipeline CI"* | Se confirmó que `package.json` usa `node --test "tests/**/*.test.js"` (incluye `contract-openapi.test.js`); se añadió el job `contract-test` en CI y se ejecutó `npm install`. | — | Verificado: `npm test` en verde. |
| **17/09/2026** | OpenCode (esta asistencia) | Alineación de la implementación HTTP con el contrato `openapi.yaml` (path params, rutas dinámicas reales faltantes) y retiro del servidor Railway no desplegado. | *"Revisa el repositorio y corrige la incongruencia entre app/ y src/; las rutas deben cumplir el contrato openapi.yaml"* | Se reestructuró `app/api/v1/` con rutas dinámicas reales (`catalogo/productos/{productoId}`, `catalogo/tiendas/{tiendaId}/productos`, `pedidos/{pedidoId}`, `notificaciones/{pedidoId}`, etc.); los handlers leen `pedidoId`/`productoId` de los path params y `tiendaId` del query; se eliminó `catalogo/route.js` y el server de Railway en `openapi.yaml`. | — | Estructura verificada contra los 10 paths del contrato. |
| **16/09/2026** | OpenCode (esta asistencia) | Generación de la evidencia de que la prueba de contrato falla ante un cambio incompatible (criterio S7 del pipeline). | *"Genera la evidencia de que tests/contract-openapi.test.js falla ante un cambio incompatible"* | Se creó `docs/evidencia-fallo-contrato-s7.md` con el procedimiento y la salida del run en rojo (`pass 22 / fail 1`, `AssertionError: Missing expected exception` en `tests/contract-openapi.test.js:69`). | — | El repo sin modificar vuelve a `23/23` en la prueba de contrato y `npm test` en verde. |
| **18/09/2026** | OpenCode (esta asistencia) | Revisión del feedback del pipeline `semana-07-evidencia-s7` (5/10 cumplen, 5 no verificados, SonarCloud no cumple): el sistema solo puede verificar nombres de archivo, no su contenido, por lo que la evidencia debe adjuntar los fragmentos. | *"Revisa el feedback del profesor, identifica qué falta para cumplir 100% y haz los documentos que faltan (sin commits)"* | Se creó `docs/evidencia-contrato-s7.md` (informe autocontenido que adjunta: paths + schemas de `openapi.yaml`, tabla contrato↔route.js↔módulo, `info.version: v1` y `git log`, fragmento del job `contract-test` en `ci.yml`, runs de Actions verdes, extracto de arc42 §6, C4 nivel 2, y plan SonarCloud). Se corrigieron conteos del README (37/37 y 23/23) y el endpoint `/api/v1/health`, se creó `docs/adr/0008` que **ratifica el ADR-0006 como aceptado** (sin editar el ADR-0006, que conserva su estado «propuesto»), y se documentó en `correcciones.md` el diagnóstico del job `sonar`. | 1) Generar un "run en rojo" real en GitHub subiendo un commit con el contrato roto — ensuciaría el historial del repositorio y el CI de la rama; se deja la repro local reproducible. 2) Modificar el ADR-0006 «aceptado» tras su ratificación — los cambios se registran en el nuevo ADR-0007, no se edita el aceptado. | Pendiente de revisión del equipo: leer el informe, ejecutar los comandos citados y cerrar el plan SonarCloud (org/proyecto + token). |

| **25/09/2026** | Muse Spark (esta asistencia, vía OpenCode) | Alineación del branch con el taller S8 (5 láminas): Azure para API+sitio, universidad para BD, Neon y Railway descartados como decisión. | *"Haz lo que hiciste antes pero con esta información"* | Se eliminaron `0009-despliegue-railway.md` y `0011-base-de-datos-neon.md` y se escribieron `0009-despliegue-azure.md` (costo × ruptura ×36, reversión) y `0011-base-de-datos-universidad.md` (Neon descartado por pausas y techo); se ajustó arc42 §2 (RES-06), §7 (Azure + universidad), §9, glosario, `semana-08.md` (§2, §6 con volumen 55k y total 0) y README. | — | Verificado local 44/44; pendiente de revisión del equipo y merge a `master` antes del cierre 28/09. |

---
La Inteligencia Artificial será utilizada únicamente como herramienta de apoyo para:
- Organización de la documentación.
- Redacción técnica.
- Generación de plantillas.
- Correcciones dentro de la organización de la documentación.
- Apoyo conceptual.
- Auditoría y revisión de errores (diagnóstico contra criterios de evaluación).

Todas las decisiones de arquitectura, diseño e implementación serán analizadas, verificadas y aprobadas por cada uno de los integrantes del equipo antes de incorporarlas al proyecto. De esta manera, se mantiene la trazabilidad de las recomendaciones recibidas y de las decisiones tomadas por el equipo.

---

# 3. Matriz de Impacto y Evaluación del uso de IA

Para medir el valor real del soporte de la Inteligencia Artificial en el proyecto, se evalúan periódicamente los siguientes indicadores:
- **Ahorro de tiempo en formato:** Reducción estimada del 40% en el tiempo dedicado a la maquetación inicial de documentos en Markdown y estructuración de índices.
- **Calidad técnica y conceptual:** Mejora en la formalización de los atributos de calidad, ayudando a estructurar de forma más clara los *trade-offs* arquitectónicos del sistema.
- **Control de calidad humano:** El 100% de los entregables han sido leídos, ajustados y aprobados por los integrantes del equipo, garantizando que reflejen fielmente la lógica real del software.
