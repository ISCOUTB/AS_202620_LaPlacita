# Plantilla de variables de entorno (versionada, sin secretos).
# Copiar a .env para desarrollo local: cp .env.example .env
# Coherente con Dockerfile:11 (NODE_ENV) y Dockerfile:16 (PORT).
PORT=3000
NODE_ENV=production
